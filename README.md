# Elijah Aniempire API

This branch contains the initial Express server scaffold integrated with Supabase.

Environment
- Copy .env.example to .env and fill in the values.

Run locally
1. npm install
2. npm run dev

Notes
- This scaffold provides auth (email/password + Discord OAuth skeleton), middleware, and stubs for the main endpoints. Continue development on branch feat/api-backend.
